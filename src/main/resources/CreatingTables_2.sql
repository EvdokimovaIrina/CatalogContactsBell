CREATE TABLE "Contact_group"(contact_id int,group_id int, CONSTRAINT contact_group_pk PRIMARY KEY (contact_id,group_id), CONSTRAINT fk_contact FOREIGN KEY (contact_id) REFERENCES "Contact" (contact_id), CONSTRAINT fk_group FOREIGN KEY (group_id) REFERENCES t_group (group_id)
);CREATE INDEX fk_contact_idx ON "Contact_group" (contact_id); CREATE INDEX fk_group_idx ON "Contact_group" (group_id);CREATE TABLE "ContactDetails" (details_id INT DEFAULT nextval ('contact_details_id_seq') NOT NULL PRIMARY KEY, details_type VARCHAR(10) NOT NULL,details_value VARCHAR(100) NOT NULL,contact_id int, CONSTRAINT fk_contact_d FOREIGN KEY (contact_id) REFERENCES "Contact" (contact_id));CREATE INDEX fk_contact_d_idx ON "ContactDetails" (contact_id);